/**
 * ToneForge - Procedural Audio Production Platform
 *
 * Generates deterministic, inspectable, and reusable sound effects
 * from recipes and seeds using Tone.js.
 */

export const VERSION = "0.1.0";

console.log(`ToneForge v${VERSION}`);
